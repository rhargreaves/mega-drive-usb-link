version: 2
jobs:
 build:
   machine: true
   steps:
     - checkout
     - run:
         name: Test
         command: ./docker-make-tests test
     - run:
         name: Build
         command: ./docker-make